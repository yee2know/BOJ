#include <vector>
#include <iostream>
using namespace std;

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	int num;
     vector<pair<int,int>> A;
     A.push_back({1,1});
	return 0;
}